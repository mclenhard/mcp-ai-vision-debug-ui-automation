{
  "name": "visual-ui-debug-agent-mcp",
  "version": "1.0.2",
  "description": "Visual UI Debug Agent MCP - MCP server for visual analysis and automated UI testing",
  "author": "samihalawa",
  "license": "ISC",
  "homepage": "https://github.com/samihalawa/visual-ui-debug-agent-mcp",
  "repository": "https://github.com/samihalawa/visual-ui-debug-agent-mcp",
  "icon": "https://raw.githubusercontent.com/samihalawa/visual-ui-debug-agent-mcp/main/publicresources/icon.png",
  "tags": [
    "ai",
    "vision",
    "debug",
    "testing",
    "automation",
    "mcp",
    "ui",
    "playwright",
    "visual-testing",
    "web"
  ],
  "compatibility": {
    "node": ">=18"
  },
  "settings": {
    "port": {
      "type": "number",
      "default": 8080,
      "description": "Port number for the server to run on"
    },
    "debugMode": {
      "type": "boolean",
      "default": false,
      "description": "Enable debug mode for verbose logging"
    },
    "headless": {
      "type": "boolean",
      "default": true,
      "description": "Run browser in headless mode"
    },
    "screenshotQuality": {
      "type": "number",
      "default": 80,
      "min": 0,
      "max": 100,
      "description": "Quality level for screenshots (0-100)"
    },
    "screenshotDir": {
      "type": "string",
      "default": "./temp",
      "description": "Directory to store screenshots"
    },
    "maxConcurrentSessions": {
      "type": "number",
      "default": 5,
      "min": 1,
      "max": 20,
      "description": "Maximum number of concurrent browser sessions"
    }
  },
  "installation": {
    "methods": [
      {
        "type": "npm",
        "command": "npm install -g visual-ui-debug-agent-mcp"
      },
      {
        "type": "docker",
        "command": "docker pull samihalawa/visual-ui-debug-agent-mcp:latest"
      },
      {
        "type": "git",
        "command": "git clone https://github.com/samihalawa/visual-ui-debug-agent-mcp.git"
      }
    ],
    "requirements": [
      "Node.js v18 or later",
      "npm v7 or later"
    ]
  }
}